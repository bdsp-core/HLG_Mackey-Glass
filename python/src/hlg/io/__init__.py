"""
File I/O for HDF5 (.hf5), MATLAB (.mat), and CSV formats.

The canonical data-loading function `load_sim_output` lives in
`hlg.io.readers` -- it replaces three near-identical copies that
previously existed across the codebase.
"""

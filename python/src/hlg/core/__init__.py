"""
Core signal-processing primitives.

Modules in this sub-package have **no local dependencies** -- they only
use third-party libraries (numpy, scipy, sklearn, mne). This makes them
the foundation layer that every other sub-package builds upon.
"""
